# Project V1(Inludes signup, login and a form fillup)
# Selenium with Java.
# Framework used TestNg with extent report and log4j fro reporting, logging.
# Download the project, go to its dir and run mvn clean install.
